---
jobs:
- name: npm install
  public: true
  serial: true
  plan:
  - get: resource-repo
    trigger: true
  - task: install
    file: resource-repo/concourse.install.yml

resources:
- name: resource-repo
  type: git
  source:
    depth: 2
    branch: develop
    uri: https://github.com/fusion-com-au/foundry-release-require-branch.git
